using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Web;
using System.Web.Script.Serialization;

namespace RejseplanenWeather.Models
{
    public class RejseFromTo
    {
       // public object getTripInfoFromTo()
       // {
            //string orig = Convert.ToString(origin);
           // string dest =  Convert.ToString(destination);
            //string url = "http://xmlopen.rejseplanen.dk/bin/rest.exe/trip?originId=" + origin + "&destId=" + destination + "&format=json";
            //string url = "http://xmlopen.rejseplanen.dk/bin/rest.exe/trip?originId="+orig+"&destId="+dest+"&format=json";
           // var client = new WebClient();
            //var content = client.DownloadString(url);
           // var serializer = new JavaScriptSerializer();
           // var jsonContent = serializer.Deserialize<Object>(content);
          //  return jsonContent;
      //  }
    }
}